import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    container: {
        backgroundColor: '#fff',
        padding: 16,
        borderRadius: 8,
    },
    greet: {
        color: '#868686',
    },
    name: {
        color: '#000',
        fontSize: 20,
    },
});

export default styles;
